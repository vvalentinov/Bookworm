namespace Bookworm.Services.Data.Tests
{
    using Xunit;

    public class BookServiceTests
    {
        //[Fact]
        //public void GetUserBooksShouldWorkCorrectly()
        //{
        //    List<Book> booksList = new List<Book>();
        //    Mock<IDeletableEntityRepository<Book>> mockBookRepo = new Mock<IDeletableEntityRepository<Book>>();
        //    mockBookRepo.Setup(x => x.AllAsNoTracking()).Returns(booksList.AsQueryable());
        //    mockBookRepo.Setup(x => x.AddAsync(It.IsAny<Book>()))
        //        .Callback((Book book) => booksList.Add(book));

        //    BooksService service = new BooksService(mockBookRepo.Object, voteRepo.Object);
        //}
    }
}
