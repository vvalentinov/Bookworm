@using Bookworm.Common
@using Bookworm.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (this.SignInManager.IsSignedIn(this.User))
    {
        var user = this.UserManager.GetUserAsync(this.User);
        var downloads = user.Result.DownloadsCount;
        var favoriteBooksCount = user.Result.FavoriteBooks.Count();
        string url = null;
        if (user != null)
        {
            url = user.Result.ProfilePictureUrl;
        }
        <li class="navbar-brand"><a asp-action="Favorites" asp-controller="FavoriteBook"><i class="fas fa-heart" title="Favorite Books"></i></a></li>
        <li class="navbar-brand"><i class="fas fa-download" title="Daily Downloads: @downloads / 10"></i></li>
        <li class="dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <img src=@url width="40" height="40" class="rounded-circle">
            </a>
            <div class="dropdown-menu text-center" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">My Profile</a>
                @if (this.User.IsInRole(GlobalConstants.AdministratorRoleName))
                {
                    <a class="dropdown-item" asp-area="Admin" asp-controller="Home" asp-action="Index" title="Admin">Admin</a>
                }
                <form class="form-inline dropdown-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@this.Url.Action("Index", "Home", new { area = string.Empty })">
                    <button type="submit" class="nav-link btn btn-link text-light justify-content-center">Logout</button>
                </form>
            </div>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
